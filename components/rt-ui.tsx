import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from "react-native";
import { useColors } from "@/hooks/use-colors";
export function Button({ label, onPress, icon = "arrow-forward", quiet = false }: { label: string; onPress: () => void; icon?: React.ComponentProps<typeof MaterialIcons>["name"]; quiet?: boolean }) { const c = useColors(); return <Pressable onPress={onPress} style={({ pressed }) => [s.button, { backgroundColor: quiet ? c.surface : c.primary, borderColor: quiet ? c.border : c.primary, opacity: pressed ? .75 : 1 }]}><Text style={{ color: quiet ? c.foreground : "#fff", fontWeight: "800" }}>{label}</Text><MaterialIcons name={icon} color={quiet ? c.foreground : "#fff"} size={18} /></Pressable>; }
export function Card({ children, style }: { children: React.ReactNode; style?: object }) { const c = useColors(); return <View style={[s.card, { backgroundColor: c.surface, borderColor: c.border }, style]}>{children}</View>; }
export function Eyebrow({ children }: { children: React.ReactNode }) { const c = useColors(); return <Text style={[s.eyebrow, { color: c.primary }]}>{children}</Text>; }
export function Loading() { const c = useColors(); return <View style={s.loading}><ActivityIndicator color={c.primary}/><Text style={{ color: c.muted }}>Menyiapkan ruang pribadimu…</Text></View>; }
const s = StyleSheet.create({ button: { minHeight: 50, borderRadius: 16, paddingHorizontal: 17, borderWidth: 1, flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, card: { borderRadius: 22, borderWidth: 1, padding: 17, gap: 9 }, eyebrow: { fontSize: 11, letterSpacing: 1, fontWeight: "800" }, loading: { flex: 1, alignItems: "center", justifyContent: "center", gap: 10 } });
